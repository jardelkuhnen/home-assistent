"""Pacote raiz do Cérebro (home-assistent-brain).

Layout flat: o diretório ``src`` é o próprio pacote Python, importável via
``src.*`` (ver ``[tool.setuptools] packages`` em ``pyproject.toml``).
"""
